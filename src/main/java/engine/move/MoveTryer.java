package engine.move;

import engine.entities.Board;
import engine.entities.copy.BoardCopier;

public class MoveTryer {
    /**
     * Given a board and a move, creates a deep clone
     * of that board and makes the specified move.
     */
    public static Board makeMove (Board oldBoard, Move move) {
        // uses BoardCopier class to create a new board and
        // BoardCopier.createCopy(oldBoard);
        // Try method in Move class to make specified move
        return null;
    }
}
