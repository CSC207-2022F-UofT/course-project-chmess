public class MoveTryer {
    public Board newBoard;
    /**
     * Given a board and a move, creates a deep clone
     * of that board and makes the specified move.
     */
    public MoveTryer (Board oldBoard, Move move) {
        // uses CloneBoard class to create a new board and
        // Try method in Move class to make specified move
    }
}
